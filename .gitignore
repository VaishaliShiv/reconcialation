# secrets — never commit real credentials
.env
*.env
!.env.example

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/

# local run artifacts
evidence_log/
recon_result*.csv
recon_summary*.json
*.xlsx
.~lock*

# local test fixtures — kept out of the repo (each user provides their own sample data)
fixtures/

# os
.DS_Store
